# platform/state/kafka/cluster/kafka-connect.yaml
apiVersion: kafka.strimzi.io/v1
kind: KafkaConnect
metadata:
  name: kafka-connect
  namespace: kafka
  annotations:
    strimzi.io/use-connector-resources: "true"
    argocd.argoproj.io/sync-wave: "3"
spec:
  version: 4.2.0
  replicas: 1
  bootstrapServers: dev-cluster-kafka-bootstrap.kafka:9092
  image: skymonil/kafka-connect-debezium:latest-kafka-4.2.0

  # 🟢 REQUIRED BY v1: These 4 properties MUST live directly under `spec` as camelCase
  groupId: connect-cluster
  offsetStorageTopic: connect-cluster-offsets
  configStorageTopic: connect-cluster-configs
  statusStorageTopic: connect-cluster-status

  config:
    # 🔴 Notice how group.id and the storage topics are completely removed from here!
    offset.storage.replication.factor: 1
    config.storage.replication.factor: 1
    status.storage.replication.factor: 1

    key.converter: org.apache.kafka.connect.json.JsonConverter
    value.converter: org.apache.kafka.connect.json.JsonConverter
    key.converter.schemas.enable: false
    value.converter.schemas.enable: false