apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: dev-infra-parent
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/skymonil/event-microservice-gitops.git
    targetRevision: main
    path: argocd-apps/dev/infra  # Points to the infra definitions
    directory:
      recurse: true # This allows it to find the .yaml files in that folder
  destination:
    server: https://kubernetes.default.svc
    namespace: argocd
  syncPolicy:
    automated:
      prune: true
      selfHeal: true