apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - prometheus/        # Assumes you have a folder for prometheus
  - grafana/           # Assumes you have a folder for grafana
  - analysis/          # 🟢 Explicitly include the analysis folder